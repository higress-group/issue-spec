name: Release

on:
  push:
    branches: [main]
    tags: ['v*']

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref == 'refs/heads/main' && 'rolling' || github.ref }}
  cancel-in-progress: false

jobs:
  package:
    name: Build and verify complete release
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v'))
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Check out exact source revision
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true

      - name: Resolve reproducible source time
        id: source
        shell: bash
        run: echo "epoch=$(git show -s --format=%ct "$GITHUB_SHA")" >> "$GITHUB_OUTPUT"

      - name: Run repository tests
        run: go test ./...

      - name: Build the complete release twice
        shell: bash
        run: |
          go run ./hack/release/cmd/package \
            --root . --out dist/release \
            --ref "$GITHUB_REF" --revision "$GITHUB_SHA" \
            --source-date-epoch "${{ steps.source.outputs.epoch }}"
          go run ./hack/release/cmd/package \
            --root . --out dist/release-repeat \
            --ref "$GITHUB_REF" --revision "$GITHUB_SHA" \
            --source-date-epoch "${{ steps.source.outputs.epoch }}"
          diff -r dist/release dist/release-repeat
          go run ./hack/release/cmd/package --verify dist/release

      - name: Verify native CLI build identity
        shell: bash
        run: |
          . dist/release/release.env
          native_dir=$(mktemp -d)
          trap 'rm -rf "$native_dir"' EXIT
          tar -xzf dist/release/publish/issue-spec_linux_amd64.tar.gz -C "$native_dir"
          "$native_dir/issue-spec" version --json > "$native_dir/version.json"
          jq -e --arg version "$RELEASE_VERSION" --arg revision "$RELEASE_REVISION" --arg channel "$RELEASE_CHANNEL" \
            '.version == $version and .revision == $revision and .channel == $channel and (.requirements_skill_content_id | startswith("sha256:"))' \
            "$native_dir/version.json"

      - name: Upload verified release candidate
        uses: actions/upload-artifact@v7
        with:
          name: release-${{ github.sha }}
          path: dist/release
          if-no-files-found: error
          retention-days: 7

  native-installer:
    name: Native installer / ${{ matrix.label }}
    needs: package
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: Shell Linux amd64
            runner: ubuntu-latest
            kind: shell
            os: linux
          - label: Shell macOS amd64
            runner: macos-15
            kind: shell
            os: darwin
          - label: PowerShell Windows amd64
            runner: windows-2025
            kind: powershell
            os: windows
    steps:
      - name: Download the exact verified release candidate
        uses: actions/download-artifact@v8
        with:
          name: release-${{ github.sha }}
          path: dist/release

      - name: Install twice from manifest with Shell
        if: matrix.kind == 'shell'
        shell: bash
        run: |
          set -euo pipefail
          chmod +x dist/release/publish/install.sh
          binary="$HOME/.local/bin/issue-spec"
          dist/release/publish/install.sh --asset-dir dist/release/publish --os "${{ matrix.os }}" --arch amd64
          first_identity=$("$binary" version --json)
          "$binary" requirements setup --help >/dev/null
          dist/release/publish/install.sh --asset-dir dist/release/publish --os "${{ matrix.os }}" --arch amd64
          second_identity=$("$binary" version --json)
          test "$first_identity" = "$second_identity"

      - name: Install twice from manifest with PowerShell
        if: matrix.kind == 'powershell'
        shell: pwsh
        run: |
          $binary = Join-Path $env:LOCALAPPDATA "issue-spec\bin\issue-spec.exe"
          ./dist/release/publish/install.ps1 -AssetDir ./dist/release/publish -Architecture amd64
          $firstIdentity = (& $binary version --json | Out-String)
          & $binary requirements setup --help | Out-Null
          ./dist/release/publish/install.ps1 -AssetDir ./dist/release/publish -Architecture amd64
          $secondIdentity = (& $binary version --json | Out-String)
          if ($firstIdentity -ne $secondIdentity) { throw "repeated native installation changed build identity" }

  publish:
    name: Attest and publish complete draft
    needs: [package, native-installer]
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v'))
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
      attestations: write
    steps:
      - name: Check out exact source revision
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true

      - name: Download verified release candidate
        uses: actions/download-artifact@v8
        with:
          name: release-${{ github.sha }}
          path: dist/release

      - name: Reverify downloaded candidate
        run: go run ./hack/release/cmd/package --verify dist/release

      - name: Read canonical release identity
        id: identity
        shell: bash
        run: |
          . dist/release/release.env
          echo "tag=$RELEASE_TAG" >> "$GITHUB_OUTPUT"
          echo "version=$RELEASE_VERSION" >> "$GITHUB_OUTPUT"
          echo "channel=$RELEASE_CHANNEL" >> "$GITHUB_OUTPUT"
          echo "revision=$RELEASE_REVISION" >> "$GITHUB_OUTPUT"
          echo "prerelease=$RELEASE_PRERELEASE" >> "$GITHUB_OUTPUT"

      - name: Attest every published asset
        uses: actions/attest-build-provenance@v4
        with:
          subject-path: dist/release/publish/*

      - name: Verify generated provenance
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          for asset in dist/release/publish/*; do
            gh attestation verify "$asset" \
              --repo "$GITHUB_REPOSITORY" \
              --signer-workflow "$GITHUB_REPOSITORY/.github/workflows/release.yml"
          done

      - name: Publish only the complete exact-revision candidate
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ steps.identity.outputs.tag }}
          RELEASE_VERSION: ${{ steps.identity.outputs.version }}
          RELEASE_CHANNEL: ${{ steps.identity.outputs.channel }}
          RELEASE_REVISION: ${{ steps.identity.outputs.revision }}
          RELEASE_PRERELEASE: ${{ steps.identity.outputs.prerelease }}
        shell: bash
        run: |
          set -euo pipefail

          guard_tag_revision() {
            git fetch --force origin "refs/tags/$1:refs/tags/$1"
            test "$(git rev-parse "$1^{commit}")" = "$2"
          }

          release_json_by_tag() {
            release_id=$(gh release view "$1" --json databaseId --jq .databaseId)
            test "$release_id" -gt 0
            gh api "repos/$GITHUB_REPOSITORY/releases/$release_id"
          }

          verify_remote_assets() {
            release_tag=${1:-$RELEASE_TAG}
            remote_dir=$(mktemp -d)
            trap 'rm -rf "$remote_dir"' RETURN
            gh release download "$release_tag" --dir "$remote_dir"
            local_names=$(find dist/release/publish -maxdepth 1 -type f -printf '%f\n' | sort)
            remote_names=$(find "$remote_dir" -maxdepth 1 -type f -printf '%f\n' | sort)
            test "$local_names" = "$remote_names"
            for local_asset in dist/release/publish/*; do
              cmp "$local_asset" "$remote_dir/$(basename "$local_asset")"
            done
            rm -rf "$remote_dir"
            trap - RETURN
          }

          if [ "$RELEASE_CHANNEL" = rolling ]; then
            test "$GITHUB_REF" = "refs/heads/main"
            git fetch --no-tags origin main
            current_main=$(git rev-parse origin/main)
            if [ "$current_main" != "$RELEASE_REVISION" ]; then
              echo "skip stale rolling candidate $RELEASE_REVISION; current main is $current_main"
              exit 0
            fi

            # GitHub keeps published_at unchanged when an existing Release is
            # edited. Stage the complete replacement as a draft, then swap the
            # Release record so the repository sidebar reflects this publish.
            replacement_tag="${RELEASE_TAG}-staged"
            if gh release view "$replacement_tag" >/dev/null 2>&1; then
              stale_replacement=$(release_json_by_tag "$replacement_tag")
              test "$(jq -r .draft <<<"$stale_replacement")" = true
              stale_replacement_id=$(jq -r .id <<<"$stale_replacement")
              gh api --method DELETE "repos/$GITHUB_REPOSITORY/releases/$stale_replacement_id"
            fi

            gh release create "$replacement_tag" --draft --target "$RELEASE_REVISION" \
              --title "latest" --notes-file dist/release/release-notes.md \
              --prerelease=false --latest=false
            replacement=$(release_json_by_tag "$replacement_tag")
            replacement_id=$(jq -r .id <<<"$replacement")
            test "$(jq -r .draft <<<"$replacement")" = true
            test "$(jq -r .target_commitish <<<"$replacement")" = "$RELEASE_REVISION"
            gh release upload "$replacement_tag" dist/release/publish/* --clobber
            verify_remote_assets "$replacement_tag"

            # The build and draft upload are deliberately long. Recheck main
            # immediately before replacing the currently published Release.
            git fetch --no-tags origin main
            current_main=$(git rev-parse origin/main)
            if [ "$current_main" != "$RELEASE_REVISION" ]; then
              gh api --method DELETE "repos/$GITHUB_REPOSITORY/releases/$replacement_id"
              echo "skip stale rolling candidate $RELEASE_REVISION; current main is $current_main"
              exit 0
            fi

            if gh api "repos/$GITHUB_REPOSITORY/git/ref/tags/$RELEASE_TAG" >/dev/null 2>&1; then
              gh api --method PATCH "repos/$GITHUB_REPOSITORY/git/refs/tags/$RELEASE_TAG" \
                -f sha="$RELEASE_REVISION" -F force=true >/dev/null
            else
              gh api --method POST "repos/$GITHUB_REPOSITORY/git/refs" \
                -f ref="refs/tags/$RELEASE_TAG" -f sha="$RELEASE_REVISION" >/dev/null
            fi
            guard_tag_revision "$RELEASE_TAG" "$RELEASE_REVISION"

            previous_release_id=
            previous_published_at=
            if gh release view "$RELEASE_TAG" >/dev/null 2>&1; then
              previous_release=$(release_json_by_tag "$RELEASE_TAG")
              previous_release_id=$(jq -r .id <<<"$previous_release")
              previous_published_at=$(jq -r '.published_at // empty' <<<"$previous_release")
              test "$previous_release_id" != "$replacement_id"
              gh api --method DELETE "repos/$GITHUB_REPOSITORY/releases/$previous_release_id"
            fi

            published=$(
              jq -n \
                --arg tag "$RELEASE_TAG" \
                --arg target "$RELEASE_REVISION" \
                --rawfile body dist/release/release-notes.md \
                '{tag_name: $tag, target_commitish: $target, name: "latest", body: $body, draft: false, prerelease: false, make_latest: "true"}' |
                gh api --method PATCH "repos/$GITHUB_REPOSITORY/releases/$replacement_id" --input -
            )
            test "$(jq -r .id <<<"$published")" = "$replacement_id"
            test "$(jq -r .tag_name <<<"$published")" = "$RELEASE_TAG"
            test "$(jq -r .draft <<<"$published")" = false
            published_at=$(jq -r '.published_at // empty' <<<"$published")
            test -n "$published_at"
            if [ -n "$previous_published_at" ]; then
              [[ "$published_at" > "$previous_published_at" ]]
            fi

            verify_remote_assets "$RELEASE_TAG"
            guard_tag_revision "$RELEASE_TAG" "$RELEASE_REVISION"
            exit 0
          fi

          test "$GITHUB_REF" = "refs/tags/$RELEASE_TAG"
          guard_tag_revision "$RELEASE_TAG" "$RELEASE_REVISION"
          if gh release view "$RELEASE_TAG" >/dev/null 2>&1; then
            draft=$(gh release view "$RELEASE_TAG" --json isDraft --jq .isDraft)
            if [ "$draft" = false ]; then
              verify_remote_assets
              remote_body=$(gh release view "$RELEASE_TAG" --json body --jq .body)
              local_body=$(cat dist/release/release-notes.md)
              test "$remote_body" = "$local_body"
              exit 0
            fi
          else
            gh release create "$RELEASE_TAG" --draft --verify-tag \
              --title "issue-spec $RELEASE_VERSION" --notes-file dist/release/release-notes.md \
              --prerelease="$RELEASE_PRERELEASE" --latest=false
          fi

          gh release upload "$RELEASE_TAG" dist/release/publish/* --clobber
          verify_remote_assets
          guard_tag_revision "$RELEASE_TAG" "$RELEASE_REVISION"
          gh release edit "$RELEASE_TAG" --draft=false --latest=false \
            --notes-file dist/release/release-notes.md \
            --prerelease="$RELEASE_PRERELEASE"
